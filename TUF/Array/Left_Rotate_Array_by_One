class Solution {
    public void rotateArrayByOne(int[] arr) {
        int n = arr.length;
        int temp = arr[0];

        for(int ind=1;ind<n;ind++){
            arr[ind-1] = arr[ind];
        }

        arr[n-1]=temp;

    }
}