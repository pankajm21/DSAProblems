class Solution {
    public int repeatedNTimes(int[] arr) {
        int n = arr.length;

        HashSet<Integer> hs = new HashSet<>();
        for(int ind=0;ind<n;ind++){
            if(!hs.add(arr[ind])){
                return arr[ind];
            }
        }

        return -1;
    }
}